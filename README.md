# CL_Scan - OCR 螢幕截圖辨識工具

簡單好用的 OCR 文字辨識工具，支援螢幕截圖即時辨識。

## 功能特色

- 🖼️ **螢幕截圖辨識**：快速截取螢幕區域並進行 OCR 文字辨識
- ⌨️ **快捷鍵支援**：預設 F3 快速啟動截圖（可自訂）
- 📋 **自動複製**：辨識結果自動複製到剪貼簿
- 🎨 **預覽功能**：截圖預覽與辨識結果對照
- 💾 **調試模式**：可保存預處理圖片檢視辨識效果
- 🌐 **內建 OCR 引擎**：不需額外安裝 Tesseract

## 使用說明

### 基本操作

1. **啟動程式**：執行 `ocr_tool.py` 或打包後的 `CL_Scan.exe`
2. **截圖辨識**：
   - 點擊「截圖辨識」按鈕
   - 或按 **F3** 快捷鍵
3. **選取區域**：拖曳滑鼠選取要辨識的螢幕區域
4. **查看結果**：辨識結果會顯示在視窗中並自動複製到剪貼簿

### 快捷鍵設定

- **預設快捷鍵**：F3
- **更改快捷鍵**：在「截圖辨識」按鈕上按右鍵，輸入新的快捷鍵（例如：F4、Control-s）

### 調試功能

點擊「💾 保存預處理圖片」按鈕可將辨識前的圖像處理結果保存下來，方便調整參數優化辨識效果。

## 系統需求

- Windows 10/11
- Python 3.8+ (開發環境)

## 開發環境安裝

```bash
# 安裝相依套件
pip install -r requirements.txt

# 執行程式
python ocr_tool.py
```

## 打包成執行檔

```bash
# 執行打包腳本
打包.bat
```

打包後的程式位於 `release/CL_Scan/` 資料夾中。

## 專案結構

```
CL_Scan/
├── ocr_tool.py          # 主程式
├── build_exe.py         # 打包腳本
├── 打包.bat              # 打包批次檔
├── requirements.txt     # Python 套件清單
├── tesseract/           # OCR 引擎資料夾
│   ├── tesseract.exe
│   └── tessdata/        # 語言包資料夾
├── LICENSE              # 授權條款
└── README.md            # 本檔案
```

## 授權條款

本專案採用 MIT License - 詳見 [LICENSE](LICENSE) 檔案

## 技術說明

### OCR 引擎
- 使用 Tesseract-OCR 5.5.0
- 支援多國語言辨識（需安裝對應語言包）
- 預設僅包含英文語言包以減少檔案大小

### 圖像預處理
程式會自動進行以下處理以提高辨識準確度：
1. 灰階轉換
2. 自動對比調整
3. 智能縮放（小圖放大）
4. 對比度增強
5. 雙重銳化
6. 二值化處理

## 常見問題

**Q: 辨識準確度不佳怎麼辦？**
- 確保截取的文字清晰、對比度高
- 避免截取過小的文字
- 使用調試功能查看預處理結果

**Q: 如何新增其他語言支援？**
- 下載對應的 `.traineddata` 檔案
- 放入 `tesseract/tessdata/` 資料夾
- 修改 `ocr_tool.py` 中的 `lang='eng'` 參數

**Q: 快捷鍵無法使用？**
- 確保程式視窗在前景
- 檢查是否與其他軟體衝突
- 嘗試更換其他快捷鍵

---

**CL_Scan** - 簡單、快速、準確的 OCR 工具
