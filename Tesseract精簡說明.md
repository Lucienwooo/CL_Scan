# Tesseract 資料夾精簡指南

## 📊 容量對比

- **精簡前**: 約 415 MB
- **精簡後**: 約 50-60 MB
- **節省**: 約 350-360 MB (87% 容量縮減)

## ✂️ 精簡內容

### 移除的檔案（不影響英文 OCR 功能）

#### 1. 多餘語言包 (~11 MB)
- `chi_tra.traineddata` - 繁體中文 (2.26 MB)
- `chi_tra_vert.traineddata` - 繁中直排 (1.74 MB)
- `jpn.traineddata` - 日文 (2.36 MB)
- `jpn_vert.traineddata` - 日文直排 (2.90 MB)
- `kor.traineddata` - 韓文 (1.60 MB)

#### 2. 訓練工具 (~2.5 MB)
這些工具僅用於訓練新的語言模型，OCR 辨識不需要：
- `text2image.exe`
- `lstmtraining.exe`
- `lstmeval.exe`
- `set_unicharset_properties.exe`
- `mftraining.exe`
- `shapeclustering.exe`
- `cntraining.exe`
- `classifier_tester.exe`
- `unicharset_extractor.exe`
- 其他訓練相關工具

#### 3. 文檔檔案 (~0.5 MB)
- 所有 `.html` 說明文件
- 不影響功能，僅為說明文件

#### 4. 額外資料 (~0.3 MB)
- `*.jar` 檔案（ScrollView GUI）
- `configs/` 資料夾
- `tessconfigs/` 資料夾
- `script/` 資料夾

### 保留的檔案（必要）

✅ **tesseract.exe** (0.08 MB)
   - OCR 主程式

✅ **所有 DLL 檔案** (~60 個，約 40 MB)
   - libtesseract-5.dll
   - libarchive-13.dll
   - 其他相依函式庫

✅ **eng.traineddata** (3.92 MB)
   - 英文語言包（必要）

✅ **pdf.ttf**
   - 字型檔案

## 🚀 使用方法

### 方法一：使用批次檔（推薦）
```bash
雙擊 "精簡tesseract.bat"
```

### 方法二：使用 Python 腳本
```bash
python 精簡_tesseract.py
```

### 方法三：整合到打包流程
執行 `一鍵打包.bat` 時會詢問是否精簡：
```
是否精簡 tesseract 資料夾? (y/n): y
```

## ⚠️ 注意事項

1. **精簡後只支援英文辨識**
   - 如需中文、日文等其他語言，請勿執行精簡

2. **精簡是單向操作**
   - 精簡後無法復原
   - 建議先備份原始 tesseract 資料夾

3. **適用場景**
   - ✅ 只需要英文/數字/符號辨識
   - ✅ 需要縮小打包檔案大小
   - ✅ 分發給終端使用者
   - ❌ 需要多語言支援
   - ❌ 需要訓練自訂模型

## 📋 精簡後的目錄結構

```
tesseract/
├── tesseract.exe           # OCR 主程式 (0.08 MB)
├── *.dll                   # 約 60 個 DLL 檔案 (~40 MB)
└── tessdata/
    ├── eng.traineddata     # 英文語言包 (3.92 MB)
    └── pdf.ttf             # 字型檔案
```

總大小: **約 50-60 MB**

## ✅ 驗證精簡結果

精簡完成後，程式會顯示：
- 原始大小
- 精簡後大小
- 節省的空間
- 保留的關鍵檔案清單

確認以下檔案存在即可正常使用：
- ✓ tesseract.exe
- ✓ tessdata/eng.traineddata
- ✓ 所有 DLL 檔案

## 🔧 如果精簡後發現問題

如果精簡後 OCR 無法正常運作：

1. **檢查關鍵檔案**
   ```bash
   python 測試打包結果.py
   ```

2. **重新取得完整版本**
   - 從備份還原
   - 或從 Tesseract 官方下載

3. **確認 DLL 完整**
   - 所有 `.dll` 檔案都應保留
   - 不要手動刪除任何 DLL

## 💡 建議

- **開發環境**: 保留完整版本（支援多語言測試）
- **發布版本**: 使用精簡版本（減少下載大小）
- **企業部署**: 視需求決定（多語言環境用完整版）

## 📞 技術支援

如遇到問題：
1. 確認 `tesseract.exe` 和 `eng.traineddata` 存在
2. 確認所有 DLL 檔案完整
3. 查看程式啟動時的 console 輸出
