# CL_Scan 語言包安裝指引

## 📦 模組化設計說明

CL_Scan 採用**模組化語言包設計**，讓您可以：
- ✅ 基礎版只有 ~70 MB（僅含英文）
- ✅ 按需下載語言包（各 15-25 MB）
- ✅ 拖放安裝，無需重新打包
- ✅ 自動偵測已安裝的語言

---

## 📂 檔案結構

打包後的 ZIP 檔案結構：
```
CL_Scan_YYYYMMDD_HHMMSS.zip
├── 使用說明.txt
├── CL_Scan/                    ← 主程式資料夾
│   ├── CL_Scan.exe
│   ├── tesseract/
│   │   └── tesseract.exe       ← OCR 引擎
│   └── tessdata/               ← 語言包資料夾
│       └── eng.traineddata     ← 英文（已內建）
│
└── language_packs/             ← 獨立語言包
    ├── CL_Scan_Lang_chi_tra.zip  ← 繁體中文
    ├── CL_Scan_Lang_jpn.zip      ← 日文
    └── CL_Scan_Lang_kor.zip      ← 韓文
```

---

## 🚀 基礎版使用（僅英文）

### 步驟 1：解壓縮
解壓縮 `CL_Scan_YYYYMMDD_HHMMSS.zip`

### 步驟 2：執行
1. 進入 `CL_Scan` 資料夾
2. 雙擊 `CL_Scan.exe`
3. 點擊「📷 開始截圖辨識」
4. 選取要辨識的區域

**此時只能辨識英文**

---

## 🌏 安裝額外語言包

### 方法一：手動安裝（推薦）

#### 安裝繁體中文
1. 解壓縮 `language_packs/CL_Scan_Lang_chi_tra.zip`
2. 取得 `tessdata/chi_tra.traineddata`
3. 複製到 `CL_Scan/tessdata/` 資料夾內
4. 重新啟動 CL_Scan

**最終結構：**
```
CL_Scan/
└── tessdata/
    ├── eng.traineddata        ← 原本就有
    └── chi_tra.traineddata    ← 新增的
```

#### 安裝日文
```
解壓 CL_Scan_Lang_jpn.zip
→ 複製 jpn.traineddata 到 CL_Scan/tessdata/
→ 重啟程式
```

#### 安裝韓文
```
解壓 CL_Scan_Lang_kor.zip
→ 複製 kor.traineddata 到 CL_Scan/tessdata/
→ 重啟程式
```

---

### 方法二：全部安裝

如果想要所有語言：

```bash
# 將所有語言包解壓後
CL_Scan/tessdata/
├── eng.traineddata       # 英文
├── chi_tra.traineddata   # 繁中
├── jpn.traineddata       # 日文
└── kor.traineddata       # 韓文
```

---

## ✅ 驗證安裝

### 程式會自動偵測
啟動 CL_Scan 時，命令列會顯示：
```
✓ Tesseract 已載入，支援語言: eng, chi_tra, jpn
```

### 測試辨識
1. 截圖包含多語言的圖片
2. 程式會自動使用所有已安裝的語言進行辨識
3. 不需要手動切換語言

---

## 📊 語言包大小

| 語言 | 檔案名稱 | 大小 |
|------|---------|------|
| 英文 | eng.traineddata | ~5 MB |
| 繁體中文 | chi_tra.traineddata | ~25 MB |
| 日文 | jpn.traineddata | ~18 MB |
| 韓文 | kor.traineddata | ~15 MB |

**總計（全部安裝）：約 63 MB**

---

## 💡 常見問題

### Q: 為什麼要分開語言包？
**A:** 
- 基礎版更小（70 MB vs 150 MB）
- 使用者只下載需要的語言
- 可以隨時新增/移除語言
- 無需重新下載整個程式

### Q: 如何知道已安裝哪些語言？
**A:** 啟動程式時會在視窗顯示：
```
✓ Tesseract 已載入，支援語言: eng, chi_tra
```

### Q: 可以只安裝中文，不要英文嗎？
**A:** 不建議。英文是 Tesseract 的基礎語言，很多符號和數字都依賴它。

### Q: 安裝語言包後需要重啟電腦嗎？
**A:** 不需要，只要重新啟動 CL_Scan 即可。

### Q: 可以移除不需要的語言嗎？
**A:** 可以！直接刪除 `tessdata/` 內對應的 `.traineddata` 檔案即可。
- 想移除日文：刪除 `jpn.traineddata`
- 想移除韓文：刪除 `kor.traineddata`
- ⚠️ 不要刪除 `eng.traineddata`

### Q: 辨識時會用哪些語言？
**A:** 程式會自動使用 **所有已安裝的語言** 進行辨識。
- 只有英文 → 只辨識英文
- 英文 + 繁中 → 辨識英文和中文
- 全部安裝 → 辨識所有語言

### Q: 可以複製到其他電腦嗎？
**A:** 可以！連同 `tessdata/` 資料夾一起複製，已安裝的語言包都會保留。

---

## 🎯 推薦配置

### 台灣使用者
```
✓ eng.traineddata      (必備)
✓ chi_tra.traineddata  (繁中)
```
**總大小：~75 MB**

### 需要多語言的使用者
```
✓ eng.traineddata      (必備)
✓ chi_tra.traineddata  (繁中)
✓ jpn.traineddata      (日文)
✓ kor.traineddata      (韓文)
```
**總大小：~130 MB**

### 只需要英文的使用者
```
✓ eng.traineddata      (預設已安裝)
```
**總大小：~70 MB**

---

## 🔧 進階：手動新增其他語言

Tesseract 支援 100+ 種語言！

### 步驟
1. 前往 Tesseract 語言包倉庫：
   https://github.com/tesseract-ocr/tessdata

2. 下載需要的 `.traineddata` 檔案
   - 簡體中文：`chi_sim.traineddata`
   - 法文：`fra.traineddata`
   - 德文：`deu.traineddata`
   - 更多...

3. 放入 `CL_Scan/tessdata/`

4. 重啟程式

---

## 📌 語言代碼對照表

| 語言 | 代碼 | 檔案名稱 |
|------|------|---------|
| 繁體中文 | chi_tra | chi_tra.traineddata |
| 簡體中文 | chi_sim | chi_sim.traineddata |
| 英文 | eng | eng.traineddata |
| 日文 | jpn | jpn.traineddata |
| 韓文 | kor | kor.traineddata |
| 法文 | fra | fra.traineddata |
| 德文 | deu | deu.traineddata |
| 西班牙文 | spa | spa.traineddata |
| 俄文 | rus | rus.traineddata |
| 阿拉伯文 | ara | ara.traineddata |

完整列表：https://tesseract-ocr.github.io/tessdoc/Data-Files

---

## 🎉 總結

這種模組化設計的優勢：
- ✅ **輕量化**：基礎版只有 70 MB
- ✅ **彈性高**：按需安裝語言
- ✅ **易維護**：拖放即可安裝
- ✅ **可攜帶**：整個資料夾都可移動
- ✅ **易更新**：只需替換單個語言包檔案

**享受輕量又強大的 OCR 工具！** 🚀
